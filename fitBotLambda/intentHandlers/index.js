const {handleQueryFood} = require('./handleQueryFood')
const {handleQueryExercise} = require('./handleQueryExercise')
const {handleLogFood} = require('./handleLogFood')
const {handleCreateWorkout} = require('./handleCreateWorkout')

module.exports = {
  handleQueryFood,
  handleLogFood,
  handleQueryExercise,
  handleCreateWorkout
}
